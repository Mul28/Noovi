{
  "name": "@noovi/head-of-growth-tools",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic Paperclip workers for Noovi Head of Growth.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "intake:start": "node intake-form/server.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.4"
  }
}
